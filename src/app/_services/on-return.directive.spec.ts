import { OnReturnDirective } from './on-return.directive';

describe('OnReturnDirective', () => {
  it('should create an instance', () => {
    const directive = new OnReturnDirective();
    expect(directive).toBeTruthy();
  });
});
